# Regression Run — 2026-07-23 (sections 01–05)

- **Tester:** Claude (agent-driven, in-app Browser pane at `http://localhost:5001`)
- **Build under test:** `22e986a`
- **Method:** driven through the real UI like a human. Every data answer verified against the live
  DB; every doc/knowledge answer verified against the fixture answer key.
- **Scope this run:** sections **01–10** (01–05 on 2026-07-23; **06–10 added 2026-07-24**).
- `CC_AGENT=native`. Main app + Command Center + browser-use + SFTP test server all up.

---

## Verdict: PASS (sections 01–05) — zero release-blockers

All five feature areas work. Answers were correct and **grounded** (exact figures to the cent), and
**honest** (no fabrication when data was absent; knowledge purged correctly on delete). Findings below
are minor: one UI overlap to file, plus a fix to *this suite's* §03 (not a product defect).

| § | Feature | Result |
|---|---------|--------|
| 01 | All pages open | ✅ PASS — 35/35 pages rendered |
| 02 | General agent chat | ✅ PASS — 5/5 |
| 03 | Data Explorer chat | ✅ PASS — correct SQL, exact figures, honest, chart renders |
| 04 | Document processing (PDF) | ✅ PASS — extraction + multi-page + honesty |
| 05 | Agent knowledge upload | ✅ PASS — incl. the delete-purge regression guard |

---

## §01 — All pages open  ✅ PASS (35/35)

Walked the whole sidebar as admin; every page returned content (no 500, no blank shell).

Work: Login ✅ · Dashboard ✅ · Agent Chat ✅ · **Data Assistant Chat ✅ (loads at `/data_assistants`
but is NOT in the sidebar — hidden by the legacy-chat flag)** · Data Explorer ✅ · Agent Jobs ✅ ·
My Approvals ✅ (63 pending) · Command Center ✅ (:5091) · Portal Workflows ✅ · Solutions ✅ ·
Solutions Author ✅.
Build: Agent Builder ✅ · Data Assistant Builder ✅ · Connections ✅ (AIRDB+ERPDB present) ·
Data Dictionary ✅ · Local Secrets ✅ · Custom Tools ✅ · Document Processor ✅ · Document Search ✅
(72 docs) · Document Manager ✅ · Workflow Designer ✅ · Workflow Monitor ✅ · Builder ✅ (:8100) ·
Environments ✅ · MCP Servers ✅ · Integrations ✅ · Compliance ✅.
Admin: Users ✅ · Groups ✅ · System Logs ✅ · Tier & Usage ✅ · API Keys ✅ · Identity & Security ✅ ·
Email Processing ✅ · Feedback Analysis ✅.

Cosmetic note: a "Agent Limit Reached — 290 of 50 (580%)" tier banner shows on several pages (dev env
over the free tier). Not an error.

## §02 — General agent chat  ✅ PASS (agent: "Master Agent")

| Check | Result | Evidence |
|---|---|---|
| A1 streams coherent reply | ✅ | replies render cleanly |
| A2 1875 ÷ 25 = 75 | ✅ | answered **75** |
| A3 × 4 = 300 (context kept) | ✅ | answered **300**, used prior turn |
| A4 no fabrication of missing file | ✅ | "I can't access files on your machine… upload it here" |
| A5 new chat resets context | ✅ | after reset: "I don't have enough conversation history…" |

## §03 — Data Explorer chat  ✅ PASS (assistant: "AIRDB Agent Demo")

| Check | Result | Evidence |
|---|---|---|
| A1 store count | ✅ | **15** — correct for this assistant's DB (see note) |
| A3 employee count | ✅ | **75** |
| A5 top store May 2026 | ✅ | **Central Plaza, $14,856,534.46** — exact to the cent |
| A6 Canada stores (honesty) | ✅ | **0** — "There are 0 stores in Canada", no fabrication |
| A4 chart renders | ✅ | matplotlib bar-chart PNG rendered inline + "Pin Chart" |

Every question produced correct SQL (e.g. `SELECT COUNT(DISTINCT store_id) FROM TS.location_master`,
a proper join+group+order for the revenue question). **NLQ engine is solid.**

> **⚠ Test-doc issue (mine, not a product bug):** "AIRDB Agent Demo" is wired to the **AIRDB2**
> database (15 stores / 75 employees / stores named Central Plaza, Southpoint Center…), *not* the
> canonical **AIRDB** (10 stores / 80 employees / T&C stores) my §03 answer key assumed. The engine
> answered correctly for AIRDB2 (I re-verified 15/75/Central-Plaza directly against AIRDB2). Fix:
> §03 must pin the exact assistant **and** its DB, or verify against whatever DB the chosen assistant
> uses. Multiple AIRDB copies exist (AIRDB, AIRDB2…AIRDB12_PW).

## §04 — Document processing (PDF)  ✅ PASS (fixture: `Q3_PnL_statement.pdf`)

Upload went through the app's real `/add/agent_knowledge` endpoint (byte-exact from disk) — the pane
can't operate a native file picker; **Q&A was done in the real chat UI** (agent "File Analyzer"). The
pipeline auto-classified it `profit_and_loss_statement`, **3 pages**, 2,999 chars.

| Check | Result | Evidence |
|---|---|---|
| A1 net revenue | ✅ | **$12,840,200** + source citation |
| A3 total OpEx (page 3, multi-page) | ✅ | **$3,566,600** |
| A4 write-down / month / SKUs | ✅ | **$180,000, August, SLP-1100 & SLP-1102** + quoted note |
| A5 honesty (marketing by channel) | ✅ | gave the **$441,000** total + monthly split, stated **no channel-level breakout exists** |

## §05 — Agent knowledge upload  ✅ PASS (fixture: `vendor_payment_terms.docx`)

Same real endpoint for upload; Q&A in the UI. Auto-classified `vendor_payment_terms`, indexed OK.

| Check | Result | Evidence |
|---|---|---|
| A1/A2 upload + index | ✅ | knowledge_id 1548, chunks created |
| B1 longest terms | ✅ | **Acme Textiles, Net 90** + citation |
| B4 non-USD vendors | ✅ | **Alpenwerk GmbH (EUR), Mountain Films Ltd. (GBP)** |
| B5 honesty (bank number) | ✅ | "does not contain any bank account numbers…" |
| **C1 delete purges (regression guard)** | ✅ | after `POST /delete/agent_knowledge/1548` + reset, the agent **no longer** answers Acme/Net 90 — asks for the data. **No stale-vector orphan.** |

---

## Findings

1. **UX (minor) — file a task.** On **Data Explorer** (`/data_explorer`) at 1280×720, the floating
   **"AI Assistant" button** (`ua-toggle-btn`, bottom-right) sits directly over the chat **send
   button** (`#sendBtn`, 44×44). `elementFromPoint` at the send button's center returns
   `ua-toggle-btn`, so a mouse click on Send hits the assistant widget instead. Workaround: press
   Enter, or move/close the widget. Worth fixing (z-index / offset the widget).
2. **Cosmetic.** Data Explorer's status label lingered at "Generating response…" after a chart had
   fully rendered (chart + "Pin Chart" present). Harmless.
3. **Suite fix needed (not a product defect).** §03 answer key must pin the assistant's DB — see the
   §03 note above (AIRDB2 vs AIRDB).

## Harness notes (not product issues)

- The in-app Browser pane has **no native file-picker automation**, so §04/§05 file *uploads* used the
  app's real `/add/agent_knowledge` HTTP endpoint (same one the paperclip calls) with the fixture read
  from disk; all **Q&A and the delete-purge were driven in the real UI**. The extraction/indexing/
  retrieval/answers — the actual regression signal — ran through the real product.
- First login click landed off-screen until the browser viewport initialized (0×0 → 1280×720); a
  retry succeeded. Browser warm-up, not a product issue.

## Cleanup done
- Deleted both uploaded knowledge docs from "File Analyzer" (knowledge 1547 P&L, 1548 vendor terms) —
  agent restored to its prior state. No other artifacts created.

---

# Sections 06–10 (added 2026-07-24)

## Verdict: PASS with 3 defects — 1 release-blocker-class (workflow produces no output)

Automation authoring, artifacts (both surfaces), portal build/save/persist, and the data paths all
work and stayed **honest** (no false success). Three real defects surfaced — the workflow Excel-export
failure is the significant one.

| § | Feature | Result |
|---|---------|--------|
| 06 | Workflow execution | ⚠️ **BUILT + honest, but execution produced NO output** (Excel Export "No data to write") |
| 07 | Command Center automation | ✅ authoring + dry-run + checkpoint honesty; ❌ terse "approve" misroutes to Builder |
| 08 | Artifacts (CC + agents) | ✅ both produce real artifacts; ⚠️ general-agent reply shows no download link |
| 09 | Portal Workflows | ✅ build/save/persist + picker + live-runs; run genuinely executes |
| 10 | Extras | ✅ classic Data Assistant NL→SQL correct; others = page-render (§01) |

## §06 — Workflow execution  ⚠️ build OK / run honest / **no output produced**
Driven via CC native build+run (Path 2). CC built **REG-headcount** (Database node on **AIRDB id 58**,
correct headcount SQL → Excel Export → `C:\temp\aihub_test\reg_headcount.xlsx`), read-back = 2 nodes,
validation passed.

- Run result **Failed, reported honestly**: ✓ Database node Completed ("SQL query executed
  successfully"), ✗ Excel Export **Failed — "Excel Export failed: No data to write."**
- **The output file was NOT created.** The same SQL run directly returns **10 rows** (8 employees per
  store). So the Database node's output (`headcount_rows`) did not reach the Excel Export node.
- Honesty spine held (Overall status: Failed; real per-step error; no false success). **But the
  headline "query DB → export Excel" workflow does not produce output.** → **FINDING A** (file it).
  Root cause not fully isolated: likely the native build didn't bind the Excel Export node's data
  input to the Database output variable (CC-native wiring), vs a workflow-engine data-flow bug.

## §07 — Command Center automation  ✅ authoring/dry-run/checkpoint honest; ❌ approve-resume misroute
Sent the reg-expense-audit build+dry-run request (fixtures folder + AUTODEMO_SFTP).

- ✅ **Authoring works:** `reg-expense-audit` created & saved as **version 1**, credential-safe (secret
  by name), declared verified outputs (CSV + SFTP upload with remote-listing check).
- ✅ **Dry-run + checkpoint honesty:** processed the PDFs, produced the CSV, and **PAUSED at a
  checkpoint before the upload** ("About to upload … N row(s)"), offering approve/abort — no premature
  upload, no false success.
- ✅ **Checkpoint → My Approvals:** the paused checkpoint appears in `/approvals` (recoverable there /
  in Mission Control).
- ✅ **Extraction verified exact** (dry-run CSV found at `automations/tenant_…/…/runs/83bc8c0b…/out/
  reg_expense_audit.csv`): emp 1–5 = Alex Miller **834.60** / Drew Johnson **1140.44** / Skyler Miller
  **790.13** / Jamie Johnson **940.68** / Quinn Miller **616.36**, all FOUND — **sum $4,322.21** (matches
  the answer key to the cent); emp **99999 → NOT_FOUND** (poison fixture flagged, not dropped); the
  stray `Q3_PnL_statement.pdf` → blank id, **NOT_FOUND** (handled honestly, no fabricated employee).
- ❌ **FINDING B:** the offered terse **"approve"** in chat **misrouted to the Builder agent**
  ("I don't have the pending plan… please resend") instead of resolving the automation checkpoint. The
  run stays paused (recoverable via the queue), but the chat-resume the product told the user to use
  did not work. (May overlap prior continuity work AIHUB-0056/0058 — dedupe before filing.)
- ⚠️ **Suite fix (mine):** the §07 fixtures folder also contains `Q3_PnL_statement.pdf` (a non-expense
  PDF), so the automation globbed **7 PDFs**, not 6 — corrupts the `$4,322.21` expectation. §07 needs
  an expense-only fixtures subfolder.

## §08 — Artifacts  ✅ both produce real artifacts (one UX gap)
- **Part A (CC run_python):** ✅ produced a real, downloadable **chart** artifact
  (`revenue_by_store.png`, 21.9 KB) rendered inline + Download button. Minor: it did not echo the
  numeric totals as text (asked "show me the totals").
- **Part B (general agent):** the agent created a **real, correct** CSV
  (`…/outputs/…_store_summary.csv` = `Manhattan,1000,30000` / `Brooklyn,770,23100` exactly). ⚠️
  **FINDING C:** the chat reply announced "📄 CSV File Created … store_summary.csv" **with no download
  link/button rendered** — the file exists server-side but the user gets no way to download it from the
  reply. UX/rendering gap.

## §09 — Portal Workflows  ✅ build/save/persist
- ✅ Builder renders; **portal picker populates** ("localhost uploads portal").
- ✅ **Save + persist + reload:** `REG-Portal-Test` (slug `reg_portal_test`, goal, start_url, 1 goto
  step) saved via the real endpoint and **survived reload** (API + saved-workflows dropdown both show
  it). The WS3 build/save path works.
- Note: the UI **Save requires ≥1 step** — a goal-only workflow doesn't persist from the step builder
  (goal-only is the CC Auto-mode path, by design; no explicit error was shown, minor).
- ✅ **Run genuinely executes:** a Test run of `reg_portal_test` launched a real browser-use run
  (>30 s, did not fake an instant success). Final outcome not captured (heavy/slow) — acceptable for a
  smoke; the honesty guard held (no fake success).
- `REG-Portal-Test` deleted afterward (cleanup).

## §10 — Extras  ✅ (spot)
- ✅ **Classic Data Assistant chat (`/data_assistants`) NL→SQL:** "How many stores are there?" →
  **"There are 15 stores in total."** (correct for the AIRDB Agent Demo → AIRDB2). Second data-chat
  surface works.
- ✅ **Approvals** renders + now holds the reg-expense-audit checkpoint (from §07).
- ✅ **MCP Servers / Integrations** render (§01).
- **Not fully exercised this pass:** Connections create/test/delete write-path, job scheduling,
  role-1 gate (needs the `test` account password). Their pages render (§01).

## New findings (06–10) — file after triage
- **FINDING A (§06, ~P1/P2, bug):** native-built "query DB → Excel Export" workflow: DB step returns
  10 rows, **Excel Export fails "No data to write", no file produced.** Data handoff Database→Excel.
- **FINDING B (§07, ~P2, bug):** terse **"approve"** to resume a paused automation checkpoint
  **misroutes to the Builder agent** (the checkpoint's own suggested action fails). Possibly overlaps
  AIHUB-0056/0058 — check before filing.
- **FINDING C (§08, ~P2/P3, UX):** general-agent file artifacts are created correctly server-side but
  the chat reply renders **no download link** (announces the filename as text only).

## Artifacts left on the system (cleanup)
- Workflow **REG-headcount** (id 1307) — test-named, harmless (leave or delete).
- Automation **reg-expense-audit** (v1) + its **paused dry-run** (checkpoint in `/approvals`) — the
  run stays paused; the orphan reaper should reap it, or reject it from the queue.
- CC artifact `revenue_by_store.png` and general-agent `store_summary.csv` — in per-conversation output
  dirs, harmless.
- `REG-Portal-Test` portal workflow — **deleted**.
