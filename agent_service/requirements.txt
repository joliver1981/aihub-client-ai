# The Agent service (agent_service) — isolated env: aihub-agent (python 3.11)
# Strategy B service (run from source in a dedicated conda env), like browser_use.
claude-agent-sdk>=0.2.132
fastapi>=0.115
uvicorn>=0.30
python-dotenv>=1.0
httpx>=0.27
requests>=2.32
PyJWT>=2.8          # shared_auth token verification
cryptography>=42    # encrypt.py Fernet decrypt of ANTHROPIC_API_KEY_ENCRYPTED
pyodbc>=5.0         # read-only ApprovalRequests/UserGroups read-through (My Work)
