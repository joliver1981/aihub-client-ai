# ═══════════════════════════════════════════════════════════════════════════
# Builder Data — Data Pipeline & Quality Micro-Service
# ═══════════════════════════════════════════════════════════════════════════
#
# This service runs alongside builder_service in the SAME virtual environment
# as the main AI Hub application. Most core dependencies (FastAPI, LangGraph,
# LangChain, etc.) are already installed via the main app / builder_service.
#
# This file lists ALL dependencies for builder_data so the environment can
# be verified or recreated independently if needed.
#
# Install:  pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════════════

# ── Web Framework (shared with builder_service) ──────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.0.0
httpx>=0.27.0
pydantic>=2.0.0

# ── LLM / Agent Framework (shared with builder_service) ─────────────────
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# ── Data Processing (likely already in main app) ────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── Database Connectivity (likely already in main app) ──────────────────
pyodbc>=5.0.0
sqlalchemy>=2.0.0

# ── Data Quality — Fuzzy Matching ────────────────────────────────────────
# rapidfuzz is the high-performance C-extension replacement for thefuzz.
# Used by the Deduplicator for fuzzy duplicate detection.
# Falls back to difflib (stdlib) if not installed, but much slower.
rapidfuzz>=3.0.0

# ── Utilities ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tabulate>=0.9.0                # Used by pandas .to_markdown() for tool outputs
