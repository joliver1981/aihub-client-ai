[x] Decide where to do development b/c some components need to run on the server itself (e.g. windows tasks) - I guess it can run locally?
[x] Figure out how to update .env files with the live application when deployed by clients
[x] Some params s/b set by client, whereas others should be known by the company
[x] Start building the UI and decide the tech
[x] Create a server for remote management demo 
[x] Create a server that will eventually run AI Hub in DEV/DEMO mode
✔ Create a demo script for presenting @done(25-06-21 16:47)
[-] Implement 2-way communication via text
[-] Implement 2-way communication via phone
[-] Implement 2-way communication via email
[x] Modify GeneralAgent class to require tools upon instantiation - tools s/b mapped to actual tools core and custom
[x] Create UI
    [x] Add security layer that handles giving users rights to agents/tools
    [x] Finish tool builder (need to add external/global config)
    [x] Finish agent builder
    [x] Modify UI to show list of available agents for chat
    [x] Modify chat interface to handle both data and regular agent types
    [x] Recreate settings (e.g users, connections, etc.)
    [x] Recreate data dictionary
[-] Provide access to FH team.. somehow..
[-] Create wireframe app store - TBD - (team decided to pause development for phase 2)
[x] Document app store vision
[x] Document security layer vision
[x] Create new approach for executing quick jobs via scehduled tasks (references a Python install directly - can replace with exe?)
[x] Create system configuration screen to replace .env file - can this file be edited directly on screen somehow?
[-] ERROR HANDLING: Reponse type == 'error'
[x] Creating new agents requries restart - Fix so they can be used on-the-fly. (due to agent dictionary in app.py is created upon initial load)
[-] Choose and apply a consistent feedback message when making updates to all pages (groups page needs to be updated)
[x] Implement Data Agents API
    [x] Add agent_id parameter
    [x] Implement dynamic database connections
[x] Implement Data Agents Chat
    [x] Modify to handle different response types (e.g. reports, images, text)
    [x] Remove from regular assistant chat 
    [x] Fix data agent class not handling various types of databases
[x] Add export feature? (low priority)
[-] Add ability to upload documents like Excel files for analysis
[x] Implement user/agent security within pages based on user defined config
[-] Implement Data Agents into Jobs for Data-driven alerting
[x] Finish tool builder so that tools can be edited - currently only creation is possible
[x] Create ADD GROUP button so that user groups can be created with UI
[x] Fix tool save so it does not go to a new page
[x] Fix text message function to remove dependency on SA API
[x] Explanation and clarification questions increases latency fix
[x] Add ability to ask for an explanation in chat
[-] Chat - when changing agents, clear chat history
[x] TODO: Research why installed versions are unstable when returning charts w/ data agents
[x] Chat - move feedback spinner to bottom of chat window so it remains visible in longer chats
[x] Add parameter that limits the size of the dataframe that can be appended to chat history
[-] Data & General Assistants: Implement session based approach for data agents to handle multiple sessions (similar to general agents but for sessions instead)
    [x] Implement for data agents (working but need to verify pandasai features work w/ multiple users)
    [-] Implement for general agents
[x] Data agents: Keep the conversation hist for first question False situations, even if they are unrelated. Currently Agent gets recreated, losing hist.
    [x] Determine if above needs to be done as follow ups seem to be working with simple ones but adding new datasets might not see outside previous...
    [x] The follow up and determining if previous dataset can be used can get confused - implment custom method with GPT and undo pandas template change.
        [x] Can you save enough information about the existing datasets to determine if a new query/dataset is needed?
[x] Data agents: Add ability to view SQL 
[x] Ability to ask the agent to explain itself
[x] Data agents: Do we need clarification questions?
[x] DATA AGENTS: If the user asks for a lot of data, create a downloadable spreadsheet (or csv)
    [x] AND send to ChatGPT with questions - NICE! - NOT NECESSARY, SOLVED WITH PANDASAI
    [x] Note: File was missing column headers - see if it is possible to include headers
[x] All chat: Fix wrapping of user messages on the right to wrap correctly
[-] Data Dictionary: deleting then saving columns seems buggy, research and fix it
[x] LLMDataEngine: Implement a function to reset the state
    [x] Implement for refreshing the page
    [x] Implement when a user changes agents in the dropdown (TBD)
[-] Implement local chat history in app.py to be per session for data agents (s/b easy and standard use case)
[x] Fix issue where scaler results do not contain enough context for follow up questions. Example, returning the # 25 doesn't indicate it is the item_id
[x] Bells & Whistles: Implement additional Handsontable featrues: formulas, coloring, export, etc.
[-] LIMITATION: Handsontable cannot handle large datasets (e.g. 9,600+ rows) and UI will open but not load the data. Is there a fix? Low priority.
[-] LOGIN ISSUE: Save user creds on logon screen is not working
[x] Improve response when no results are returned from a query
[x] Next add a check to see if a new query is necessary or if follows ups can be handled by Pandas alone
    [x] Try assuming follow ups and only getting new data when required
[x] IMPORTANT: Implement recursion __getstate__ functions in V2 classes to avoid the current recursion errors
[x] Add api key verification to aihub-call app
[x] Data Agent: Fix chart export causing app to crash/restart (verify with installed apps)
[x] Data Agent: Fix link to large datasets not working with installed apps
[x] Data Agent: Add flag to indicate whether the last question was answered or required additional information
[-] BUG: Sometimes no tables are selected after long chats (even though they should), and the app cannot recover and all questions after fail..
[-] BUG: List index out of range error after asking to format dollars that dont exist and then following up with asking to format with commas
[-] _is_analytical_query_required_v2: Needs additional context - added recent conv hist if follow up, need to verify and/or even add more context
[-] BUG: Pandas code sometimes fails. Could it be an empty "result" statement at the end of some code generation? Also, references df indexes that don't exist
    [-] TODO: To avoid referencing DF indexes that don't exist, try modifying prompts in PandasAI code
[x] Analytical prompt: Should I remove "use last dataset where possible" from the prompt? Is it needed now with all the other improvements?
    [x] REMOVED:  and if possible, use the most recent dataframe to answer the question
[-] BUG (Data Assistant): Sometimes formatting questions get applied to older datasets earlier in the conversation
✔ Data Agent: Handle no results by differentiating failed queries versus queries that correctly yield no results @done(25-06-21 16:53)
✔ TODO: Solve for certain follow up answers not being related to analytical queries, rather SQL queries like when asked for links between tables (likely rare?) @done(25-06-21 16:53)
✔ TODO: TABLE SELECTION PROMPT IS INFERIOR AND NEEDS TO BE UPDATED LIKE THE LATEST PROMPTS - it returns no tables too often, needs context and explanation @done(25-06-21 16:53)
[-] TODO: Flow out the entire conversation process and prompts to better understand the impact and strategy and loop holes
[-] TODO: Verify conversation history is working in all prompts
[-] BUG: See "Special Scenario 5" and figure out why the AI decided to use the analytical layer, which populated everything with zeros but used correct structure
✔ TODO: LIMIT THE # OF COLUMNS ALLOWED IN CHAT IN ADDITION TO ROWS @done(25-06-21 16:53)
✔ BUG: Scheduled jobs don't work @done(25-06-21 16:52)
[x] SCENARIO WARNING: User asks question -> AI asks for more info -> AI confirms the request for confirmation -> Confirmation response gets fully processed
    - User: why did the months start in August?
    - Assistant: please confirm the sales are zero before Aug
    - Assistant -> Runs query to confirm no records before Aug
    - Assistant -> Formats response: "I can confirm there are not sales before Aug"
    - Assistant (as User): I can confirm there are not sales before Aug
    - ISSUE: The final response from the AI should have been returned to the user, not issued as another question...
[-] BUG: Initial question returned is dataset enough = No w/ Q = items w/ inventory and no sales (SQL Server EDW)
Analytical check results (v2):
    {
      "dataset_is_sufficient": "no",
      "explanation": "The dataset contains duplicate Item_Key values, which may confuse the user. The user likely expects a list of unique items that have inventory but no sales this month. Further processing to remove duplicates and possibly provide additional context (like item names or descriptions) would make the dataset clearer and more useful.",
      "confidence": 90
    }
    
[-] BUG: Check the files inside the folder \\aihubstoragedev.file.core.windows.net\shared-files-dev\data\processing\failed for the text Store#. If it appears more than once in a file, fix the file and create a new file with the same name and append the text "_fixed" to the file name. Also, send a text to James Oliver to notify him of the situation.
    - Analysis: The prompt successfully fixed and created a new file; however, it failed to lookup the users phone# and instead used the name as the parameter.
    - TODO: Perhaps add a failsafe where if the format is not a phone# add a lookup function that uses the user name to find the phone#

[-] TODO: Upon install COPY user entered account creds to user_config.py, so users never have to edit the main .env file
[-] TODO: /chat/email uses a single agent(1) for demo purposes. Need to develop function to choose the correct agent (subject?), or create designated email agents.
    - Once above is completed, delete the creation of GeneralAgent(1) at the top of app.py
✔ TODO: Once multi-tenant feature is fully validated, delete commented legacy code and TENANT_ID from config.py that gets tenant id @done(25-06-21 16:55)
[-] TODO: Convert all database config interaction to new class
[-] BUG: Workflows fail to save to database: The attempted operation failed because the target object 'AIHUB.dbo.Workflows' has a block predicate that conflicts with this operation.
[-] TODO: Add + New Workflow button to menu (s/b able to use existing function that clears the canvas upon opening an existing workflow)
[-] IMPLEMENT REAL EMAIL AGENT! Removed temporary code at top of app.py: monitoring_agent = GeneralAgent(1) (remove before deployment or tenants will not work!!!)
[-] Finish implementing passing outputs to nodes in workflow tool
[X] THOUGHT: Does returning helpful helpful error messages telling the AI what to do next improve results? NOTE: This seems to work - added as error message for no docs, suggesting additional fields to try
    - For example, if it attempts to search for a document or table using the incorrect name, can telling it to use the get_names tool work?
    - This could be the best approach for OOB tools as I notice sometimes the AI does not use the correct tools in the correct order and it guesses sometimes
[X] TODO: How can I solve for getting necessary information that spans multiple pages? NOTE: Added auto get next 'n' pages parameter & check completeness function
[-] CODE CLEANUP: Doc API has separate copies of functions (eg get_db_connection) that exist in AppUtils due to avoiding installing OpenAI Python module with Anthropic but this s/b resolved
[-] *** Add friendly column label in data dictionary so the AI can provide users with a friendly name
✔ *** Add setting to trun off "show query" option in chat interface for Data Assistants @done(25-06-21 16:50)
✔ When job is executed via new scheduler make sure it gets added to execution history at the bottom of the job details page @done(25-06-21 16:53)
☐ DEPLOYMENT TODO's
    - Create exe for app_doc_api.py - this is used for executing document jobs from the main app via api calls (due to anthropic models)
    - Create exe for app_jss_main.py - this is the robust scheduler
    - Create exe for ExecuteDocumentJob.py - this executes document jobs from the queue
    - Verify exe for ExecuteQuickJob.py - how will this work with the new scheduler?
    - Integrate Quick Jobs w/ new scheduler
    - BUG: Delete knowledge doc removes from agent but does not actually delete records from the database tables (it only inactivates by design)
    - [RESOLVED]BUG: Agent knowledge is only active after reloading agents - this needs to be fixed so it is available right after processing the document
        - NOTE: Need a way to reload individual agents instead of all agents - this is a perfect example, just adding knowledge
[-] LLMQueryEngine._load_query(): Gracefully return and handle database connection errors, etc. - consider evaluating error w/ mini model to determine if hard stop and default messaging is necessary
[-] CRITICAL: After installing the application, you MUST GRANT RIGHTS TO THE LOG FOLDER TO PREVENT UNEXPECTED BEHAVIOR DUE TO LOG ATTEMPT FAILURES
[-] Change the default "temp_chart.png" file name to something dynamic to prevent potential collisions (it only needs for a short time however...)
☐ One-time document schedules still sometimes add duplicate entries
